require 'rubygems'
require 'picturama'
require "rake"
require "rake/testtask"

picturama = Gem::Specification.find_by_name 'picturama'
Dir["#{picturama.gem_dir}/lib/tasks/*.rake"].each { |task|
  load task
}